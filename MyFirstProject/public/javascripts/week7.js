function numbers() {
    let sHTML = "<b>";
    sHTML += 1 + "<br>" + 2 + "<br>" + 3 + "<br>" + 4 + "<br>";
    sHTML += "</b>";
    clientSideContent.innerHTML = sHTML;
}

function multiply() {
    inputNumber.value = inputNumber.value * 3;
}

let car = { make: "Ford", model: "Mondeo", age: 2 };

function showVehicles() {
}